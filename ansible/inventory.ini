[research]
# Remote host — replace with your server's IP or hostname
# research-server ansible_host=192.168.1.100 ansible_user=pi

# Local deployment (same machine running ansible)
localhost ansible_connection=local
