[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: requires real API keys (skip with -m "not integration")
    slow: tests that may take >30 seconds
addopts = -m "not integration"
